   def minimumSum(self, num: int) -> int:
        s=str(num)
        k=[]
        for i in range(len(s)):
            k.append(int(s[i]))
        k.sort()
        a=k[0]*10 +k[3]
        b=k[1]*10 + k[2]
        return a+b
